name: internationalizations_workflow
description: An application to demo the internationalizations workflow for Flutter.
version: 1.0.0+1

environment:
  sdk: ">=2.10.0-0.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Internationalization support.
  flutter_localizations:
    sdk: flutter
  intl: 0.16.1
flutter:
  generate: true
